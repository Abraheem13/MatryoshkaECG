# ---- core (required) ------------------------------------------------------
torch>=2.2.0
numpy>=1.26.0
scipy>=1.11.0
scikit-learn>=1.4.0
pandas>=2.1.0
PyYAML>=6.0
tqdm>=4.66.0

# ---- data -----------------------------------------------------------------
wfdb>=4.1.0
h5py>=3.10.0

# ---- probing (Section V-D) ------------------------------------------------
neurokit2>=0.2.7          # physiological descriptor extraction
joblib>=1.3.0

# ---- figures --------------------------------------------------------------
matplotlib>=3.8.0

# ---- hardware benchmarking (Section V-C) ----------------------------------
# All optional: benchmark_hardware.py degrades gracefully and records the
# reason in benchmark.json if any of these are missing.
thop>=0.1.1               # MAC counting
onnx>=1.15.0              # ONNX export
onnxruntime>=1.17.0       # portable-runtime latency
nvidia-ml-py>=12.535.0    # GPU energy via NVML (import name: pynvml)
